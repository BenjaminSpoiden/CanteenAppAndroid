<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <ImageView
        android:id="@+id/allergies_image"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/sugar" />

    <TextView
        android:id="@+id/message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/lightGreen"
        android:gravity="center"
        android:padding="16dp"
        android:text="Your order is ready. It will arrive soon!"
        android:textColor="#28140c"
        app:layout_constraintBottom_toBottomOf="@id/allergies_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toRightOf="@id/allergies_image"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toEndOf="@id/allergies_image"
        app:layout_constraintTop_toTopOf="@id/allergies_image"
        app:layout_constraintVertical_bias="0.75" />
</merge>